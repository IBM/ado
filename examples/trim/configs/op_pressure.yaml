# Copyright (c) IBM Corporation
# SPDX-License-Identifier: MIT
spaces:
  - space-c8717f-3a68bf
operation:
  module:
    operationType: characterize
    operatorName: trim
  parameters:
    batchSize: 5 # added
    autoGluonArgs:
      fitArgs:
        time_limit: 20
        presets: medium
        # tabularPredictorArgs:
        # problem_type: regression
        # eval_metric: mean_absolute_percentage_error
    iterationSize: 5
    outputDirectory: trim_models
    stoppingCriterion:
      enabled: true
      meanThreshold: 0.9
      stdThreshold: 0.75
    noPriorsParameters:
      targetOutput: pressure # it is not properly set here
    targetOutput: pressure
    taskId: null
    taskValue: null
