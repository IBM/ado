[project]
name = "trim"
version = "0.1.0"
requires-python = ">=3.10,<3.13"
dependencies = ["pandas",
                "numpy",
                "scikit-learn",
                "autogluon.tabular[catboost,xgboost]==1.5"]


[tool.setuptools]
packages = ["trim"]


[project.entry-points."ado.operators"]
trim = "trim.operator"
